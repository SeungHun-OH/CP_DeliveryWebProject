<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapper">
<insert id="insertUser" parameterType="com.delivery.user.model.UserVO">
	INSERT INTO CAP_DELIVERY_USER (USER_ID, USER_NAME, USER_BIRTH, USER_GENDER, USER_PWD, USER_PHONE, USER_ADDR, USER_ADDR2, USER_EMAIL)
	VALUES (	${user_id}
				, ${user_name}
				, ${user_birth}
				, ${user_gender}
				, ${user_pwd}
				, ${user_phone}
				, ${user_addr}
				, ${user_addr2}
				, ${user_email}
	)
	
</insert>
</mapper>